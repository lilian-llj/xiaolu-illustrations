---
name: xiaolu-illustrations
description: 生成小鹿风格的中文正文配图。用于用户要求为中文文章、帖子、小红书、公众号、方法论、流程、结构、状态、隐喻或观点生成"小鹿""配图""正文配图""文章插图""配图建议""shot list"等任务；默认使用小鹿 IP（幼年梅花鹿+紫睡衣）、油画棒蜡笔手绘、低饱和马卡龙配色、浅奶油底、温暖治愈但结构清楚的视觉风格。生图走火山方舟 ARK + Seedream 垫图管线，不用内置生图工具。
agent_created: true
---

# 小鹿正文配图

## 核心定位

为中文文章设计和生成 16:9 横版正文配图。目标不是商业插画、PPT 信息图或幼稚卡通，而是把文章里的关键判断、流程、结构、状态或隐喻，变成一张温暖、治愈、有创意、可读但不说明书的油画棒手绘解释图。

默认视觉 IP 是"小鹿"：幼年梅花小鹿，奶呼呼短幼体型，紫底白星星睡帽 + 紫色睡衣粉蕾丝包边，爱心腮红、圆黑豆豆眼、鼻梁三点白。小鹿必须参与画面的核心动作，不能只是站在旁边当装饰。

> 方法论骨架改编自 Ian 的 lilian-xiaohei（MIT License），特此致谢。IP 形象"小鹿"为独立原创角色。

## ⭐ 生图方式（与其他配图 skill 的最大差异）

**不要用内置 image_gen / ImageGen 生角色图。** 本 skill 的生图走脚本管线：

```bash
cd <工作目录>
PYTHONUTF8=1 ARK_API_KEY=<用户的Key> python <skill目录>/scripts/xiaolu_gen.py \
  --topic "主题" --structure "结构类型" \
  --core-idea "核心意思" --composition "具体画面" \
  --elements "元素1 / 元素2" --labels "标注1 / 标注2" \
  --ref-image "<skill目录>/assets/小鹿-标准像-v1.png"
```

三条铁律：
1. **任何出现小鹿的图，必须用官方标准像垫图**（`--ref-image` 指向 `assets/小鹿-标准像-v1.png`）。纯文字 prompt 裸生角色会漂移（额头冒白点、长出尾巴、鹿角变大），已被多轮实测证明。
2. **ARK_API_KEY 从环境变量取**，没有就先问用户要，不要硬编码进文件。
3. Python 用 managed venv：`C:/Users/rome/.workbuddy/binaries/python/envs/default/Scripts/python.exe`，命令前加 `PYTHONUTF8=1` 防中文乱码。

## 先读这些参考

按任务需要读取，不要一次塞满上下文：

- `references/style-dna.md`：风格 DNA、颜色、文字、禁忌。
- `references/xiaolu-ip.md`：小鹿 IP 的形象、性格、动作库和禁忌（含标准像使用规则）。
- `references/composition-patterns.md`：结构类型、原创隐喻方法和反复刻规则。
- `references/prompt-template.md`：单张生图提示词模板（含角色锁定前缀）。
- `references/qa-checklist.md`：生成后检查和迭代规则。
- `assets/examples/`：8 张示例图，只作低频视觉校准，不要照抄构图、物件或标注。
- `assets/小鹿-标准像-v1.png`：官方标准像，**每次生图必带的垫图基准**。

## 工作流

### 1. 消化正文

先读用户给的正文、链接、Markdown 文件或截图内容。提炼：

- 核心观点是什么
- 哪些段落承担认知转折
- 哪些内容适合用图解释
- 哪些地方只适合文字，不需要图

不要平均配图。优先选择"认知锚点"：核心判断、两个断点、输入输出闭环、分流、前后对比、一素材多用、承接路径、常见坑、角色状态变化。

### 2. 先出配图策略

如果用户只是说"分析怎么配图 / 思考哪些地方需要配图"，先给 shot list。每张图写清楚：

- 放在哪个段落后
- 图的主题
- 核心意思
- 结构类型
- 小鹿在图里做什么
- 建议元素
- 建议中文标注词

默认 4-8 张。文章很短时 1-3 张；长文也不要轻易超过 9 张。

### 3. 单张生成

用户明确要求"生成 / 输出 / 做图"时，不要停下来等确认；用上面的脚本管线每张单独生成，全部带标准像垫图。批量时可参照项目里的 `batch_examples.py` 模式（复用 gen_one + CHAR_LOCK 前缀 + 支持按编号重跑）。

每张图的 composition 开头必须带角色锁定前缀（见 `references/prompt-template.md` 的 CHAR_LOCK 段），特别强调：

- 三点白只在鼻梁，额头绝对无白点，全脸白点不超过 3 个
- 不露出尾巴
- 弯月抱枕是可选道具：小鹿需要腾出手做动作时直接省略

不要复刻示例图构图。每次都从当前文章重新发明一个温暖但成立的隐喻。

### 4. 检查与迭代

生成后对照 `references/qa-checklist.md`。因为 agent 会话可能看不了图，**必须把图 present 给用户当裁判**，重点让用户核对：

- 小鹿是不是同一只（紫睡帽、紫睡衣粉蕾丝、爱心腮红、豆豆眼）
- 额头有没有多冒白点、有没有尾巴
- 概念表达清不清楚

已知顽固问题（Seedream 对梅花鹿的视觉先验）：额头白点、尾巴偶发。重跑 1-2 次通常可解决；仍压不住时用 PIL 后处理抹掉。

### 5. 保存交付

图保存到工作目录 `assets/<topic-slug>-illustrations/`，按 `01-topic.png` 顺序命名。保留原始生成文件，不要覆盖已有资产。交付时用 present_files 发图给用户。

## 输出口径

生成前的策略输出要短而准。生成后的交付要包含：生成了几张、每张用途、保存路径、哪些最稳哪些可选。不要长篇解释风格理论；让图自己说话。
